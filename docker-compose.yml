name: "docusaurus"
services:
    dev:
        build:
            context: .
            target: prod
        ports:
            - "3000:3000"
        volumes:
            - .:/app
        environment:
            - NODE_ENV=production